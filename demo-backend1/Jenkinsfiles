pipeline {
    agent any
    
    stages {
        stage("Build & making tar file") {
            steps {
                sh "cd demo-backend1"
                sh "cd demo-backend1 && tar -cvf javatar.${img_tag}.tar target"
            }
        }
    }
}
